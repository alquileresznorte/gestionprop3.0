name: Backup Firebase GestionProp

on:
  schedule:
    # 12:00 Argentina (UTC-3) = 15:00 UTC, todos los sábados
    - cron: '0 15 * * 6'
  workflow_dispatch: {} # también se puede correr a mano desde la pestaña "Actions"

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar toda la base de Firebase
        run: |
          FECHA=$(TZ='America/Argentina/Buenos_Aires' date +'%Y-%m-%d')
          curl -s "https://gestion-prop-default-rtdb.firebaseio.com/.json" -o "backup-$FECHA.json"
          echo "FECHA=$FECHA" >> $GITHUB_ENV

      - name: Subir el backup al repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Guardar el archivo en la carpeta backups/
        run: |
          mkdir -p backups
          mv "backup-${FECHA}.json" "backups/backup-${FECHA}.json"

      - name: Commitear el backup
        run: |
          git config user.name "backup-bot"
          git config user.email "backup-bot@users.noreply.github.com"
          git add "backups/backup-${FECHA}.json"
          git commit -m "Backup automático ${FECHA}" || echo "Nada nuevo para commitear"
          git push
